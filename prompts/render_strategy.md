# Render Strategy Task

{{skill_render_strategy}}

Target platform:

{{platform_spec}}

For every scene define composition, duration, crop, layer motion, subtitle treatment, transition, audio relationship and fallback. Do not output renderer-specific code.
