# 清爽知识卡片模板

请把已在剪映中制作好的 9:16 母版草稿完整复制到 `draft_template/`。母版中的素材和文本占位名必须与 `placeholders.json` 一致。

## 母版占位命名

在剪映母版中，把 8 个镜头素材依次命名为 `scene_01_placeholder` 至 `scene_08_placeholder`。配音和音乐命名为 `voiceover_placeholder`、`bgm_placeholder`；标题类文本使用 `hook_text`、`keyword_text`、`cta_text`、`cover_text` 和 `subtitle_style`；品牌素材使用 `brand_logo`、`ending_qr`、`teacher_avatar`、`course_card`。名称必须完全一致，缺少任一素材时系统会给出警告并转入兼容素材包模式。
