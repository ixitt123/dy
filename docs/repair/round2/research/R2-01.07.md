# R2-01.07 修改前全网研究：动态大字主预览与 BGM seek 同步

## 检索记录

- 检索时间：2026-08-05 19:43–19:48（Asia/Shanghai）。
- 关键词：`HTMLMediaElement currentTime seeking seeked event`、`multiple media elements synchronization currentTime`、`timeupdate 250ms synchronization`、`requestAnimationFrame media sync`、`Chrome play promise user gesture`、`web-platform-tests media currentTime seeked`。
- 范围：优先 WHATWG HTML Living Standard、W3C 媒体定时规范、MDN、Chrome 官方文档和 Web Platform Tests 上游；未采用博客、论坛或搜索摘要作为结论依据。

## 一手来源与适用性

### 1. WHATWG HTML Living Standard：媒体时间与精确 seek

- 来源：[HTML Standard — Media elements](https://html.spec.whatwg.org/multipage/media.html)。
- 要点：设置 `currentTime` 会更新正式播放位置并启动 seek；`seeking` 表示定位仍在进行，`seeked` 表示定位完成；需要精确位置时应设置 `currentTime`，不能用可能降低精度的近似 seek 代替。
- 适用性：主预览拖动必须把同一个目标秒数写入旁白与 BGM；验证不能只看滑杆文本，应同时记录主时钟、旁白 `currentTime`、BGM `currentTime`、`seeking/seeked` 状态。

### 2. MDN：`currentTime`、`duration` 与可 seek 范围

- 来源：[HTMLMediaElement.currentTime](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/currentTime)、[Audio and video delivery](https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Audio_and_video_delivery)。
- 要点：`currentTime` 单位为秒，设置后媒体从目标时间开始或继续；时长来自 `duration`，可用 `seekable` 判断可定位范围。
- 适用性：BGM 比旁白长时目标应为主预览时间对 BGM 时长取模；每次写入前须确认媒体至少已取得 metadata，测试应允许浏览器完成异步 seek 后再判定误差。

### 3. W3C：不要用低频 `timeupdate` 驱动同步

- 来源：[Requirements for Media Timed Events](https://www.w3.org/TR/media-timed-events/)。
- 要点：`timeupdate` 的约 250ms 更新粒度和不同浏览器的时序差异不适合精确同步；同步渲染可轮询主媒体 `currentTime`，并使用 `requestAnimationFrame()` 获得更平滑时钟。
- 适用性：保留旁白作为主媒体时钟、`requestAnimationFrame()` 驱动画布的架构；不增加 `timeupdate` 驱动的第二时钟。只在开始、拖动提交、刷新恢复和明显漂移时校准 BGM。

### 4. Chrome 官方：播放 Promise 与用户手势

- 来源：[Chrome for Developers — Autoplay policy](https://developer.chrome.com/blog/autoplay/)。
- 要点：有声 `play()` 可能因缺少用户手势而拒绝，界面状态不能假设播放已成功，应处理 Promise；真实验证不应关闭自动播放策略。
- 适用性：回归必须使用 fresh Chrome 和物理点击主播放按钮；分别确认旁白、BGM 的 `paused/currentTime`，不能仅凭按钮文字宣称播放。

### 5. Web Platform Tests 与 WHATWG 上游讨论

- 来源：[web-platform-tests/wpt](https://github.com/web-platform-tests/wpt)、[WHATWG HTML issue #609](https://github.com/whatwg/html/issues/609)。
- 要点：WPT 是浏览器共享的 Web 平台测试基线；上游讨论明确指出浮点时间和媒体容器时间尺度会使逐帧定位存在舍入，验收应使用合理误差而非不稳定的严格单点比较。
- 适用性：本项按“主/BGM 时间差在明确阈值内”验收，保存原始小数和 seek 完成状态；不以 `>0.5` 一类与 fixture 时长偶合的单边断言代替同步误差。

### 6. HTTP Range：媒体随机定位的传输前提

- 来源：[RFC 9110 — HTTP Semantics](https://httpwg.org/specs/rfc9110.html)、[MDN — HTTP range requests](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Range_requests)、[MDN — Configuring servers for media seeking](https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Formats/Configuring_servers_for_Ogg_media)、[Node.js `fs.createReadStream`](https://nodejs.org/api/fs.html#fscreatereadstreampath-options)。
- 要点：支持字节范围的服务器应声明 `Accept-Ranges: bytes`；合法单范围返回 `206`、准确 `Content-Range` 和范围长度，越界返回 `416`；媒体播放器依赖 Range 获取尚未下载的 seek 目标。Node 文件流的 `start/end` 均为包含端点。
- 适用性：`/api/kinetic-text/file` 当前忽略 `Range` 并总是整文件 `200`，这会让 `preload=metadata` 的 BGM 在真实二次随机定位时长期停在 `seeking=true`。修复应只在此受控本地文件路由实现严格单字节范围，拒绝多范围/畸形/越界输入，不放宽路径边界。

## 本地调用链与修改前证据

- `previewDuration()` 以已解码旁白时长为主时长；`seekValueToTime()` 把 0–1000 滑杆映射到主时长。
- `anchorPreviewClock()` 保存唯一主预览时钟；`previewClockTime()` 优先旁白 `currentTime`，seek 锁定期间使用锚定时钟。
- `syncPreviewMediaTime()` 同时写旁白、BGM 和背景视频时间；BGM 使用 `targetTime % bgm.duration`。
- `beginPreviewSeek()` 在拖动开始时记住播放状态并暂停所有媒体；`finishPreviewSeek()` 再次提交目标并按原状态恢复播放。
- `previewTick()` 使用 `requestAnimationFrame()` 更新画布，但播放过程中没有周期性校正 BGM 漂移；开始与 seek 提交是当前主要同步点。
- 修改前 `test-kinetic-bgm-player-browser.mjs` 使用真实安全 WAV 和 headless Chrome，主预览播放后 BGM 能推进，但把滑杆设为 500 并派发 `input/change` 后在第 97 行失败：`拖动主预览时间后可见 BGM 播放条没有同步`。
- 失败证据：`.data/repair-evidence/R2-01.07/20260805-194256/pre-fix/01-kinetic-bgm-player.log`；SHA-256 `CD0566E669E9C526D5B49116ED8D5E537A56443CA3EF056B5C2484B4E2572FD5`。
- 增强调试证据进一步区分两层问题：第一次旧断言读取发生在 `seeking=true`，等待 seek 完成后主/BGM 均到 0.6 秒；但真实物理第一次拖动、暂停、第二次物理拖动后，BGM 在 15 秒内仍保持 `currentTime=0`、`seeking=true`、`readyState=1`，证明仍有真实传输层随机定位缺陷。
- 二次物理 seek 失败报告：`.data/repair-evidence/R2-01.07/20260805-194256/diagnostic-1/tests/kinetic-bgm-player-browser.json`；报告 SHA-256 `3DF2C6AD919FF4F46D9383AF42AD56A465E9D24D9E8154CC2D38616DBD803C83`。

## 采用方案

1. 先把回归改为可诊断且自清理：保存失败 JSON、实际主/旁白/BGM 时间、`seeking/paused/readyState`、滑杆值和截图，失败也保留证据。
2. 使用物理鼠标拖动主滑杆，覆盖播放中第一次 seek、暂停、暂停态第二次 seek、恢复播放和刷新后的再次 seek；fresh Chrome 不绕过自动播放策略。
3. 以旁白为唯一主时钟，BGM 为从时钟；每次 seek 提交后等待媒体 `seeked` 或时间进入容差，再恢复并核对误差。
4. 若诊断证明 BGM seek 在 `readyState` 尚未取得 metadata 时被跳过，则缓存目标并在 `loadedmetadata/canplay` 补交；若证明播放恢复后独立媒体发生漂移，则只在超过容差时校正，避免每帧反复 seek。
5. 在受控 Kinetic 文件端点补齐严格单范围响应，并新增 API 级 `0-15`、开放尾段、后缀范围、畸形/多范围和越界用例；保留现有文件解析与允许目录边界。
6. 保留可见 BGM 原生控件和既有音量同步，不引入第二状态表。

## 未采用方案及原因

- 用 `timeupdate` 驱动主画布或每 250ms 强行覆盖：官方资料明确提示粒度和跨浏览器时序不足。
- 每个动画帧都写 BGM `currentTime`：会持续触发 seek、造成音频卡顿并掩盖真正的时钟关系。
- 关闭 Chrome 自动播放策略：不能代表用户真实环境。
- 改用 Web Audio 全量解码：会失去现有可见原生播放器/流式加载边界，修改范围过大。
- 只放宽旧断言或只检查按钮文字：不能证明第一次/第二次 seek、暂停、恢复和刷新后的真实同步。
- 修改 MoneyPrinterTurbo：与本项无关且明确禁止。

## 研究结论

当前前端实现方向符合标准：旁白主时钟、rAF 画布、BGM 从时钟和显式 seek 提交都是正确基线。增强证据排除了“仅等待异步 seek 即可”的解释：第二次真实拖动稳定卡住的直接根因是受控媒体文件端点缺少 Range 支持。应在该路由做最小、安全的单范围实现，并用 API 边界测试与三次物理 seek/暂停/刷新回归共同证明；禁止用更宽松断言掩盖失败。

## 实施结果

- `server/routes/kinetic-text-routes.js` 现对受控输出文件声明 `Accept-Ranges: bytes`；合法单范围返回 `206`、准确 `Content-Range/Content-Length` 并以包含端点的文件流传输；畸形、多范围、零后缀和越界范围返回 `416` 与 `Content-Range: bytes */<size>`。
- 原有路径解析、文件存在性判断、MIME、下载文件名和 `no-store` 边界保持不变；未增加任意路径输入，也未修改 MoneyPrinterTurbo。
- 新真实回归通过正式 TTS handoff 持久化链创建四件套项目，完成播放中第一次物理拖动、暂停、暂停态第二次物理拖动、恢复播放、刷新恢复、刷新后第三次物理拖动；三次主/BGM 时间差均在明确容差内。
- Range API 验证：`bytes=0-15`、`bytes=-16`、`bytes=16-` 均返回准确 `206` 和真实字节；畸形、多范围、越界均返回 `416`，测试 handoff 与项目在 `finally` 中按精确 ID/受控目录清理。
- 通过报告：`.data/repair-evidence/R2-01.07/20260805-194256/diagnostic-3/tests/kinetic-bgm-player-browser.json`；SHA-256 `CF4AC64A50B6194AF15AADD6C810ADFBA731127392BDC727C953710CA236AC49`。
- 首次/二次 seek 截图 SHA-256：`7E778BB056D5AB3F7C5215B103DC639270D9FB60DB0F5B6980F8EE5F0EB27169`；刷新后截图 SHA-256：`5DF0F3659E96BD014C6D45E96CDB528D593678153A4243F1F927135AE6D6CBF2`。
- 兼容结果：`test-kinetic-atomic-bgm-browser.mjs` 通过；`test-kinetic-bgm-recovery-browser.mjs` 通过，报告 SHA-256 `D522F0D8E5B42489C1BB27E80526DD2515878865114D5A0D83BD51F60C2664F3`，测试启动的恢复服务已关闭。
