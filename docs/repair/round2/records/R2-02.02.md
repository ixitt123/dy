# R2-02.02｜维修与复验记录

## 第 1 次完整维修并完整复验｜失败

- 时间：2026-08-05T10:42:00+08:00
- 执行电脑：B
- 分支：`repair/b-r2-02-02`
- 开工基线：`ade30b3bc3887de8096948ece9fc3a625e4eacdd`
- 真实证据：`D:\cs1\dy-b-primary-20260805-102410\.data\repair-evidence\R2-02.02\20260805-104200\attempt-1`
- 失败点：`pnpm.cmd audit --prod` 使用官方 npm registry 执行后退出码为 `1`。
- 准确结果：`ip-address` 的三条公告已全部从审计结果消失；审计仅剩 `fast-uri@3.1.4`（R2-02.01）、`hono@4.12.27`（R2-02.05）和 `brace-expansion@2.1.3`（R2-02.06）。
- 根因：当前执行规格把全仓生产依赖审计的零退出码绑定到单个供应链维修项；三个仍处于独立未开始状态的安全项会使本项无法单独获得 audit 零退出。
- 已排除原因：
  - 不是 `ip-address` 仍为旧版本：`pnpm.cmd why ip-address` 只显示 `10.3.1`。
  - 不是前导零绕过仍可达：`node test-ip-address-leading-zero.mjs` 通过，上游解析器和应用守卫均拒绝公告样例，且 DNS、请求计数为零。
  - 不是 DNS/连接目标漂移：公网域名测试把 `93.184.216.34` 从已校验 DNS 结果原样传给连接层；歧义 DNS 结果在请求前拒绝。
  - 不是既有 SSRF 回归：`node test-ssrf-guard.mjs` 为 16/16。
  - 不是一般业务回归：`pnpm.cmd run check:gate`、`repair:r2:check`、`repair:r2:test-supervisor`、`git diff --check` 均为零退出。
- 本次代码结果：依赖升级与防护实现有效；因规定的全局审计命令非零，本次不得登记完成。
- 后续约束：不重复运行相同维修消耗第 2 次；待 `R2-02.01`、`R2-02.05`、`R2-02.06` 清除剩余公告后，再进行第 2 次完整复验。

### 证据文件

- `01-leading-zero.log`：目标动态回归，退出码 0。
- `02-ssrf-guard.log`：既有 SSRF 动态回归，退出码 0。
- `03-pnpm-audit-prod.log`：全局生产审计，退出码 1，准确列出三个其他编号的剩余公告。
- `04-check-gate.log`：完整门禁，退出码 0。
- `05-round2-check.log`：第二轮总表检查，退出码 0。
- `06-supervisor.log`：第二轮监督器自测，退出码 0。
- `07-git-diff-check.log`：差异检查，退出码 0。
- `08-pnpm-why-ip-address.log`：活动依赖树唯一版本 `10.3.1`，退出码 0。
- `verification-result.json`：结构化失败清单、根因、已排除原因和全部命令结果。

## 第 2 次完整维修并完整复验｜失败

- 时间：2026-08-05T11:06:58+08:00
- 执行电脑：B
- 分支：`repair/b-r2-02-02`
- 开工提交：`9d4330ef175022fcc1470be657e3cbf85974c573`
- 真实证据：`D:\cs1\dy-b-primary-20260805-102410\.data\repair-evidence\R2-02.02\20260805-110454\attempt-2`
- 失败点：`pnpm.cmd run repair:r2:test-supervisor` 的“B atomically activates the first non-overlapping A/B pair”断言失败。
- 根因：激活自测的 `activationReadyPlan` 直接把 `R2-02.02` 重置为“未开始、0 次”，却没有清除总表中既有的 `ATTEMPT-1-R2-02.02` 失败日志；检查器准确报告“attempts column requires 0 failure records, found 1”。
- 已排除原因：
  - `node test-ip-address-leading-zero.mjs` 退出码 0，依赖版本、公告样例、DNS 前拒绝和连接目标固定均通过。
  - `node test-ssrf-guard.mjs` 退出码 0，既有 SSRF 动态回归通过。
  - `pnpm.cmd run check:gate` 退出码 0，完整项目门禁通过。
  - `pnpm.cmd run repair:r2:check` 退出码 0，真实总表状态有效。
  - `git diff --check` 退出码 0；`pnpm.cmd why ip-address` 退出码 0，活动版本仍唯一为 `10.3.1`。
- 本次代码结果：R2-02.02 业务修复仍有效；失败仅来自监督器第二处合成 fixture 未复用既有日志隔离函数，不能把该控制缺陷掩盖为业务完成。
- 后续约束：先在独立控制分支修复激活 fixture 并更新协作基线，再把已审核控制提交应用到 B 分支；下一次完整维修并复验为第 3/4 次。

### 证据文件

- `01-leading-zero.log`：目标动态安全回归，退出码 0。
- `02-ssrf-guard.log`：既有 SSRF 动态回归，退出码 0。
- `03-check-gate.log`：完整项目门禁，退出码 0。
- `04-round2-check.log`：真实第二轮总表检查，退出码 0。
- `05-supervisor.log`：监督器激活 fixture 失败的完整输出，退出码 1。
- `06-git-diff-check.log`：差异检查，退出码 0。
- `07-pnpm-why-ip-address.log`：活动依赖树证明，退出码 0。
- `verification-result.json`：结构化失败点、根因、已排除原因和证据文件清单。

## 第 3 次完整维修并完整复验｜通过

- 时间：2026-08-05T11:18:42+08:00
- 执行电脑：B
- 分支：`repair/b-r2-02-02`
- 复验提交：`bbc5b5cafad8707aa3afdd23bb581e23543fd707`
- 真实证据：`D:\cs1\dy-b-primary-20260805-102410\.data\repair-evidence\R2-02.02\20260805-111108\attempt-3`
- 完成结论：`ip-address` 活动依赖树已统一为 `10.3.1`；前导零歧义 authority 在 DNS 与请求之前拒绝；无效 DNS IP 在 socket 请求前拒绝；正常公网域名把已校验地址原样固定给连接层，解析、DNS 和连接目标一致。
- 完整命令结果：
  - `node test-ip-address-leading-zero.mjs`：退出码 0。
  - `node test-ssrf-guard.mjs`：退出码 0。
  - `pnpm.cmd run check:gate`：退出码 0。
  - `pnpm.cmd run repair:r2:check`：退出码 0。
  - `pnpm.cmd run repair:r2:test-supervisor`：退出码 0。
  - `git diff --check`：退出码 0。
  - `pnpm.cmd why ip-address`：退出码 0，活动版本唯一为 `10.3.1`。
- 完成登记：完成记录器校验每条预期命令、输出 SHA-256、安全证据和回滚证据后成功；总表更新为“完成 3/4”，对应 B assignment 原子更新为 `closed`，无锁文件残留。
- 审计边界：全仓 `pnpm audit --prod` 仍由 `R2-02.07` 汇总门禁负责，`R2-02.01`、`R2-02.05`、`R2-02.06` 不再错误阻塞本项，但其漏洞仍未被声明解决。

### 完成证据文件

- `completion-result.json`：预期/实际命令、退出码、输出路径与 SHA-256、验证模式和证据索引。
- `completion-gates.json`：七道门结论。
- `verification-summary.md`：安全验收结论。
- `rollback-plan.md`：最小回滚边界与用户数据保护要求。
- `01-leading-zero.log` 至 `07-pnpm-why-ip-address.log`：全部真实命令输出。
