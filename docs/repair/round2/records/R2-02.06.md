# R2-02.06 维修记录：`brace-expansion` 资源耗尽

## 基线与边界

- 执行机：B；分支：`repair/b-r2-02-06`。
- 开工协作基线：`224ea07a67b8d6eab4790eb389f6e932dc272277`。
- 研究提交：`44d69b80f7a039b5c618b3a7bc684b0406f96809`。
- 任务包：`.data/repair-evidence/R2-02.06/20260805-223530/current-work-packet.md`。
- MoneyPrinterTurbo 指针保持 `d994b15fc8f4b8a9eeb7d43cc8aed4a2a0e2c3f2`，未修改子模块内部或 gitlink。
- 未上传 `.data`、设置、密钥、Cookie、Token、SQLite、下载、素材或导出。

## 根因与修复

- 根 `pnpm.overrides` 将传递依赖固定在 `brace-expansion@2.1.3`；该版本仍受 GHSA-rgw5-rvv9-x895 / CVE-2026-69152 影响，逗号备选项会累计无界中间数组，填充序列未正确应用总长度上限。
- 精确升级到官方修复版 `2.1.4` 并更新冻结锁文件；未采用不必要的 5.x 主版本迁移。
- 新增 `test-brace-expansion-dos.mjs` 并接入 `check:gate`。危险输入仅在 96 MiB 堆、6 秒超时的隔离子进程执行。
- 动态覆盖填充序列、逗号备选、链式扩展和零步长，并验证 `archiver.directory()` 对 Windows 可用的 brace/glob 风格字面文件名不作展开。
- 真实业务只通过 `archive.directory(sourceDir, false)` 归档目录，不接收不可信 glob 模式，因此无需修改业务源码。

## 第 1 次完整维修复验：通过

- `node test-brace-expansion-dos.mjs`：通过；活动包与锁文件均为 2.1.4。
- 填充序列：约 35ms，输出 3,980,199 字符；逗号备选：约 234ms，输出 3,999,981 字符；均未超过 4,000,000 字符限制。
- 链式扩展和零步长均及时结束；真实 ZIP 保留 `literal-{a,b}.txt`、`literal-[abc].txt`、`literal-+([abc]).txt` 字面名称，并清理临时目录。
- `pnpm.cmd run check:gate`：通过。
- `pnpm.cmd run repair:r2:check`：通过。
- `pnpm.cmd run repair:r2:test-supervisor`：31/31 通过。
- `git diff --check`：通过。
- 官方 npm 审计中 `brace-expansion` 公告数为 0；仍有一项 Hono 公告，归属独立项目 R2-02.05，未误报为本项解决。

## 复验说明与发布边界

- 修复前目标文件不存在，`node test-brace-expansion-dos.mjs` 稳定返回 `MODULE_NOT_FOUND`；这是建立回归基准，不计完整维修次数。
- 编写测试时两次针对 Windows 文件名与上游零步长语义的断言修正发生在正式完整门禁前，不构成完整维修复验。
- 本项仅进入第二轮协作维修基线，不声明 PR、CI 或正式发布完成。
- 回滚证据：`.data/repair-evidence/R2-02.06/20260805-223530/attempt-1/rollback-plan.md`。
