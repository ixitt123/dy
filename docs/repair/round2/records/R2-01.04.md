# R2-01.04 维修记录：TTS BGM 可见播放器加载与任务切换

## 基线与边界

- 执行机：B；分支：`repair/b-r2-01-04`。
- 开工协作基线：`a5f667fe6bb97d4527d7a8d3896023bd0b496f0d`。
- 修复提交：`9edb53c1a738f7ac5ec2f7b92b43ce0fdcef9bf5`。
- 任务包：`.data/repair-evidence/R2-01.04/20260805-190802/current-work-packet.md`。
- MoneyPrinterTurbo 指针：`d994b15fc8f4b8a9eeb7d43cc8aed4a2a0e2c3f2`；未修改子模块内部或指针。
- `.data`、SQLite、设置、密钥、下载和本地素材未提交。

## 修改前研究与失败判据

- 完整研究：`docs/repair/round2/research/R2-01.04.md`。
- 一手依据：WHATWG 媒体资源选择、MDN 动态换源、Chrome 自动播放策略、W3C/WAI 媒体控件要求。
- 修改前旧回归固定依赖另一台机器 SQLite 的任务 `93/94`；B 主机没有 BGM `94`，在第 20 行失败，不能进入播放器断言。
- 失败日志：`.data/repair-evidence/R2-01.04/20260805-190802/pre-fix/01-bgm-player.log`；SHA-256 `96FC931161026293A1D0F9F3FB4551344AED800D14519CF028E183A479987103`。
- 业务根因：有 BGM 任务切到无 BGM 任务时只显示缺失面板，没有先清除唯一 BGM 播放器；Chromium 在简单移除 `src` 后还可能保留只读 `currentSrc`。

## 修复

- 所有已完成且无有效 BGM 的激活/刷新路径统一先调用 `clearTtsBgmPreview()`，再显示缺失状态。
- 清理函数先暂停、移除源并 `load()`，再用无源克隆元素原位替换，消除 Chromium 残留 `currentSrc`；变量仍指向唯一播放器，没有引入第二状态所有者。
- `test-tts-bgm-player.mjs` 改为自包含安全 WAV fixture：两组已确认精确父子 BGM、一条显式无 BGM 旁白、fresh headless Chrome、真实物理点击、A→B→无 BGM 与刷新复验。
- fixture 默认在 `finally` 中通过正常删除 API 清理；失败时也清理并写 JSON 证据。

## 第 1 次完整维修复验

- 结果：通过；无需第 2 次，权威次数 `1/4`。
- `node test-tts-bgm-player.mjs`：通过；报告 SHA-256 `3587E3872C299C2B117756CBB90D01089D9D2BC465B6CC6CBE679B26A60A6A72`。
- BGM A、BGM B 和刷新后 BGM A 均在 30 秒内可见，带原生控件，物理点击后 `currentTime > 0.15`，`currentSrc` 精确指向目标 BGM ID。
- 切换 BGM B 后不含 BGM A URL；切换无 BGM 后播放器隐藏、缺失状态可见、`src/currentSrc` 为空、旧音频暂停。
- `test-tts-bundle-labels.mjs`、`test-tts-bgm-option.mjs`：兼容回归通过。
- `pnpm.cmd run check:gate`、`repair:r2:check`、监督器 31/31、`git diff --check`：全部通过。
- 正式 fixture 任务 `79–91` 已清理，SQLite 查询为空；端口 `8787/8080/9231/9233/9235` 均离线。

## 证据与回滚

- 正式证据根：`.data/repair-evidence/R2-01.04/20260805-190802/attempt-1/`。
- `completion-result.json` SHA-256：`4BA90394135C1A73D4A960904CA78A039BCAFB3EBE9ADDB01C597A4DA409A9D9`。
- 主播放器截图 SHA-256：`8C77B4CBCFB73FCAF524C22D1E0C7E8AECE707D1CC3E6BC2D61455DBB8508B79`。
- 回滚仅在新分支反向提交 `9edb53c1a738f7ac5ec2f7b92b43ce0fdcef9bf5`，不改写历史；没有数据库迁移或用户数据删除。
