# R2-01.13｜launcher 日志可靠性与轮转：维修记录

- 执行日期：2026-08-05
- 执行电脑：A
- 分支：`repair/a-r2-01-13`
- 开工基线：`1b36a6d831bb65c19df2ad51682b981a0ac5c447`
- 上游开工基线：`origin/repair/a-r2-01-13` 同为 `1b36a6d831bb65c19df2ad51682b981a0ac5c447`
- MoneyPrinterTurbo 指针：`d994b15fc8f4b8a9eeb7d43cc8aed4a2a0e2c3f2`，未修改
- A 机完整维修复验：第 1 次失败；第 2 次通过；A 的两次额度已用完
- 当前结论：形成候选提交，等待 B 唯一审核、登记两次结果并决定合并；A 不更新权威总表。

## 开工与控制面

最初共同基线 `5b09a6a8dc83a9921098d3421e1583c9e1620271` 因 R2-01.12 指向 B 本地绝对证据路径，A 无法生成任务包。A 没有创建、伪造或复制 B 的 `.data`。B 修复跨机器证据收据后，A 按授权执行 `fetch` 和 `merge --ff-only`，精确同步到 `1b36a6d831bb65c19df2ad51682b981a0ac5c447`。

任务包随后成功生成：

- `.data/repair-evidence/R2-01.13/20260805-115219/current-work-packet.md`
- 状态：进行中
- 次数：0/4
- A 允许路径：本记录列出的 5 个文件

## 强制研究

修改业务源码前已完成微软与 Node.js 官方资料检索，记录在：

- `docs/repair/round2/research/R2-01.13.md`

结论是 `ForAppending`/`appendFile` 不能替代并发协调；轮转检查、倒序移动和追加必须在同一个跨进程互斥区完成。未采用原生 `LockFileEx` 桥接、复制后清空、无限等待和修改 `ui-server.mjs`。

## 修复前失败

- 命令：`node test-launcher-log-rotation.mjs`
- 退出码：1（预期的修复前失败）
- 准确断言：`VBS failures must leave a structured launcher log record.`
- 证据：`.data/repair-evidence/R2-01.13/20260805-115219/pre-fix/test-launcher-log-rotation.txt`
- 结论：原日志没有可解析且字段完整的 VBS 错误记录；并发、轮转和 Node 事件也没有可靠实现。

## 修改内容

### `start-ui-hidden.vbs`

- 把本地化日期加自由文本改成 ASCII 安全的逐行 JSON。
- 固定字段：`timestamp`、`event`、`project`、`pid`、`url`、`runId`、`message`。
- 使用 `.data/launcher-log.lock` 目录协调 VBS 与 Node 跨进程写入。
- 有限重试并恢复陈旧锁；任何路径都释放锁。
- 默认活动日志 256 KiB，保留 3 个倒序轮转备份；非法环境配置回退安全默认值。
- 缺 Node/依赖/入口、Node 交接开始、失败和完成均写结构化事件。
- 保留 R2-01.12 的可见错误、非零退出和停止半残服务行为。

### `launch-ui.mjs`

- 使用与 VBS 相同的锁目录、大小限制、备份命名和结构化字段。
- 写入 Node 启动、预检错误、复用现有服务、启动新服务和异常事件。
- 复用记录实际 URL；新服务记录真实子进程 PID，并有界等待本次更新的 `ui-server.url`。
- 日志错误只发出警告，不覆盖原始启动成功/失败语义。
- 保留 detached、隐藏窗口、无 shell、现有服务复用和 `ui-server.mjs` 启动参数。
- 没有修改 `ui-server.mjs`；错误项目/陈旧 URL 身份校验仍留给 R2-01.14。

### `test-launcher-log-rotation.mjs`

- 真实 Windows `cscript.exe` VBS 错误。
- Node 入口错误。
- 新服务启动及真实子进程 PID/URL。
- 真实本地 HTTP 服务复用。
- 24 个 Node 并发写入，24/24 唯一 `runId` 保留。
- 16 个 VBS 并发写入，16/16 唯一 `runId` 保留。
- 1200 字节阈值、2 个备份的连续轮转。
- 60 秒陈旧锁恢复，恢复后继续写入并释放锁。
- 保存各场景真实 `launcher.log` 和汇总 JSON。

## 第 1 次完整维修复验｜失败

本项针对性回归、上一项兼容回归、语法、第二轮总表检查和差异检查均通过；完整门禁失败：

```text
test-ip-address-leading-zero.mjs
actual:   10.2.0
expected: 10.3.1
```

诊断确认：B 的新基线已经把 `package.json`/`pnpm-lock.yaml` 固定到 `ip-address 10.3.1`，但 A 快进后本地 `node_modules` 仍保留旧版 `10.2.0`。该失败不归因于本项 launcher 源码，但完整门禁未通过，因此如实计为 A 第 1 次失败。

- 结果：`.data/repair-evidence/R2-01.13/20260805-115219/attempt-1/verification-result.json`
- SHA-256：`4EAB8DDCDAA14A64BC9BBD1012DE8B59C8F9AD295AA027B3F1A01AC41A8206BE`
- 当次 launcher 汇总 SHA-256：`7C576CE16B3F41F86E1609D103620E69E22E7990515B1F7B0BDF608084A1469F`

## 第 2 次完整维修复验｜通过

先执行 `pnpm.cmd install --frozen-lockfile`，只同步本地依赖目录；没有修改 `package.json`、`pnpm-lock.yaml`、SSRF 源码或测试。安装后 `test-ip-address-leading-zero.mjs` 通过。

| 命令 | 结果 |
|---|---|
| `pnpm.cmd install --frozen-lockfile` | 通过；锁文件不变，本地安装 `ip-address 10.3.1` |
| `node test-launcher-log-rotation.mjs --evidence-dir .data/repair-evidence/R2-01.13/20260805-115219/attempt-2/launcher-log` | 通过 |
| `node test-launcher-missing-dependency.mjs` | 通过 |
| `node test-ip-address-leading-zero.mjs` | 通过 |
| `pnpm.cmd run repair:r2:check` | 通过；73 项，活动项只有 R2-01.13 |
| `git diff --check` | 通过；仅行尾提示，无空白错误 |
| `pnpm.cmd run check:gate` | 通过；退出码 0，耗时 78.3 秒 |

真实汇总：

- VBS 错误：退出码 2，1 条结构化记录
- Node 错误：退出码 3，2 条结构化记录
- 新服务：退出码 0，记录子进程 PID 与 `http://127.0.0.1:48787`
- 复用：退出码 0，记录真实随机本地 HTTP URL
- Node 并发：24/24
- VBS 并发：16/16
- 轮转：活动日志 581 字节，2 个备份，无第 3 个备份
- 陈旧锁：恢复成功

证据：

- 结果：`.data/repair-evidence/R2-01.13/20260805-115219/attempt-2/verification-result.json`
  - SHA-256：`B95A98DDE69CCE139A03F178AF5E8304E35ADCD3ACABE79CB5612B91B622C98B`
- 七道门：`.data/repair-evidence/R2-01.13/20260805-115219/attempt-2/seven-gates.json`
  - SHA-256：`19F91EBBD40520CD9262E17850F037C49930783140099B8F9E5D392268C08E92`
- launcher 汇总：`.data/repair-evidence/R2-01.13/20260805-115219/attempt-2/launcher-log/launcher-log-verification.json`
  - SHA-256：`EC42FB6DC1DC847C1BE2F4DE169C2B1FE4F5D00F2BEFE5C086D16722FBA49ACF`
- 各场景原始日志：同目录下 `vbs-error/`、`node-error/`、`new-server/`、`reuse/`、`concurrent-node/`、`concurrent-vbs/`、`rotation/`

## 七道门审计

| 门禁 | 状态 | 说明 |
|---|---|---|
| A 代码 | 通过 | 针对性回归、兼容回归、语法、完整代码门禁和差异检查通过 |
| B 逻辑 | 通过 | 锁覆盖大小检查、倒序轮转和单行追加；并发唯一事件未丢 |
| C 功能 | 通过 | 错误、新启动、复用、并发、轮转和恢复旅程均有真实运行证据 |
| D 真实使用 | 通过 | 使用真实 Windows Script Host、多进程和真实本地 HTTP 服务 |
| E 安全 | 通过 | 未新增 shell 拼接、网络权限或敏感日志字段；安全同步门禁通过 |
| F 数据与回滚 | 通过 | 仅 `.data/launcher.log*`；未修改设置、SQLite、历史任务、媒体或用户资产 |
| G 发布 | 不适用 | A 只提交候选；B 负责唯一审核、权威登记和协作基线合并 |

完整 41 项 F01–F24 补充矩阵未在 A 再次运行：本次是隔离候选交接，不是 B 的权威完成或正式发布决定。B 审核完成状态时必须按其审计规则决定并记录完整矩阵结果；A 不把本地代码门禁等同于正式发布。

## 数据影响、风险与回滚

- 未修改 `ui-server.mjs`、总表、assignments、监督器、发布配置、package/lock 或 MoneyPrinterTurbo。
- 未提交 `.data`、本地依赖、设置、密钥、Cookie、Token、SQLite、下载或媒体。
- 日志使用项目目录 `.data/launcher.log` 及最多 3 个备份；默认最大总量约 1 MiB。
- 陈旧锁阈值默认 30 秒；锁失败只警告，不把日志故障升级为启动故障。
- R2-01.14 仍需处理错误项目/陈旧 `ui-server.url` 身份，不应在本项提前声明解决。
- 回滚只回滚本候选提交中的 5 个允许文件；现有 `.data/launcher.log*` 可保留作为诊断记录，不影响旧启动器运行。

## B 统一审核与登记

1. 按权威总表登记 A 第 1 次完整复验失败和第 2 次通过；A 的两次额度已用完。
2. 核对候选提交只含 5 个允许文件，MoneyPrinterTurbo 指针不变。
3. 在 B 干净审核工作树复跑目标回归、R2-01.12 兼容回归、`check:gate`、`repair:r2:check` 和 `git diff --check`。
4. 抽查 40 个并发 `runId`、轮转备份、陈旧锁恢复、PID/URL字段和原始证据哈希。
5. 审核不通过时由 B 按总次数继续第 3 次；禁止把 A 再拉回进行第 3 次源码维修。
6. 审核通过后只有 B 可以更新总表、生成跨机器证据收据并合入协作基线。
