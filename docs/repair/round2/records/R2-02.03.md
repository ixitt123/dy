# R2-02.03｜CIDR 后缀与规范化维修记录

## 开工信息

- 执行电脑：B
- 分支：`repair/b-r2-02-03`
- 开工基线：`f8dd23481646c1dbaf4f7ae3e1e3f60f700afa1c`
- 任务包：`.data/repair-evidence/R2-02.03/20260805-115356/current-work-packet.md`
- 次数预算：B 直接复杂项，最多 4 次完整维修复验。

## 修复前状态

- `node test-cidr-normalization.mjs`：退出码 1，准确失败为目标测试文件不存在。
- 当前 `ip-address` 实际解析版本已经是 R2-02.02 锁定的 `10.3.1`，高于 GHSA-4xrf-jv44-h6hh 的首个修复版本 `10.2.2`。
- 现有应用守卫已能拒绝内部 URL 和带 CIDR 的非法 DNS 结果，但没有公告级独立测试，也未纳入全仓门禁。

## 根因

R2-02.02 的统一依赖升级与 DNS 严格校验已经消除了当前运行时绕过，但 R2-02.03 的专属回归缺失。若以后 override、锁文件、pnpm 实际链接或应用 URL/DNS 校验回退，`check:gate` 无法依据公告样例阻止合入。

## 修改

1. 新增 `test-cidr-normalization.mjs`：
   - 核对根 override 与锁文件均为 `ip-address@10.3.1`；
   - 沿实际 `express-rate-limit` pnpm 依赖链接加载运行时包；
   - 动态执行公告的 IPv4/IPv6 `/0`、短前缀分类样例；
   - 验证公网地址不误判；
   - 验证内部 URL 阻断、公共 `/0` 路径放行、带 CIDR DNS 结果在请求前拒绝。
2. 把 `node test-cidr-normalization.mjs` 加入 `check:gate`。
3. 未修改 `server/core/ssrf-guard.mjs`：现有代码已满足本项边界，无需制造重复分类实现。

## 第 1 次完整维修复验

- 证据：`.data/repair-evidence/R2-02.03/20260805-120100/attempt-1/`
- `node test-cidr-normalization.mjs`：通过。
- `node test-ssrf-guard.mjs`：16/16 通过。
- `pnpm.cmd run check:gate`：通过。
- `pnpm.cmd run repair:r2:check`：通过。
- `pnpm.cmd run repair:r2:test-supervisor`：通过。
- `git diff --check`：通过。
- 结论：第 1 次完整维修复验通过，无需第 2 次。

## 安全与回滚

- 没有修改用户数据、设置、SQLite、密钥、Cookie、Token、本地素材或导出文件。
- 没有修改 MoneyPrinterTurbo 内部或指针。
- 本项回滚只移除新增测试、门禁命令和本项文档；不得回退 `ip-address@10.3.1`，因为它属于已完成 R2-02.02 的安全基线。
