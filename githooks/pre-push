#!/bin/sh
echo "[pre-push] 正在运行正式发布闸门..."
pnpm run check:gate
status=$?

if [ "$status" -ne 0 ]; then
  echo "[pre-push] 闸门失败，已阻止推送。请把上面的错误发给 Codex。" >&2
  exit "$status"
fi

echo "[pre-push] 闸门通过，允许推送。"
exit 0
