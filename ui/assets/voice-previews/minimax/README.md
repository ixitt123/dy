# MiniMax preset voice previews

The Xiaohei app writes each preset preview here once after a MiniMax API key is
configured. Later playback uses the saved MP3 directly and does not call the API.

Preview filenames are stable SHA-1 prefixes derived from `provider:voice_id`.
