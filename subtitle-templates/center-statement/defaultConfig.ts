import type { SubtitleTemplateConfig } from "../types";
const config: SubtitleTemplateConfig = { fontFamily: "Microsoft YaHei", fontSize: 82, primaryColor: "#FFFFFF", accentColor: "#A7C7FF", position: { x: 50, y: 50 }, maxLines: 2, animationSpeed: 0.9, backgroundOpacity: 0, outlineEnabled: false, shadowEnabled: true };
export default config;
