import type { SubtitleTemplateProps } from "../types";
import { activeSegmentIndex } from "../helpers";
export default function CenterStatementTemplate({ segments, currentTimeMs, config }: SubtitleTemplateProps) { const index = activeSegmentIndex(segments, currentTimeMs); if (index < 0) return null; const segment = segments[index]; const enter = Math.min(1, (currentTimeMs - segment.startMs) / 180); return <div className="subtitle-center-statement" style={{ position: "absolute", left: `${config.position.x}%`, top: `${config.position.y}%`, width: "76%", transform: `translate(-50%,calc(-50% + ${(1 - enter) * 18}px)) scale(${.96 + enter * .04})`, opacity: enter, color: config.primaryColor, fontFamily: config.fontFamily, fontSize: config.fontSize, fontWeight: 720, lineHeight: 1.25, textAlign: "center", letterSpacing: ".01em" }}>{segment.text}</div>; }
