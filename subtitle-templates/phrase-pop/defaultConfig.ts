import type { SubtitleTemplateConfig } from "../types";
const config: SubtitleTemplateConfig = { fontFamily: "Microsoft YaHei", fontSize: 98, primaryColor: "#FFFFFF", accentColor: "#FFB12B", position: { x: 50, y: 61 }, maxLines: 2, animationSpeed: 1.05, backgroundOpacity: 0, outlineEnabled: true, shadowEnabled: true };
export default config;
