import type { SubtitleTemplateProps } from "../types";
import { activeSegmentIndex } from "../helpers";
export default function CaptionCardTemplate({ segments, currentTimeMs, config }: SubtitleTemplateProps) { const index = activeSegmentIndex(segments, currentTimeMs); if (index < 0) return null; const segment = segments[index]; const enter = Math.min(1, (currentTimeMs - segment.startMs) / 160); return <div className="subtitle-caption-card" style={{ position: "absolute", left: `${config.position.x}%`, top: `${config.position.y}%`, maxWidth: "76%", transform: `translate(-50%,-50%) scale(${.96 + enter * .04})`, opacity: enter, borderRadius: ".36em", padding: ".55em .72em", background: `rgba(16,18,22,${config.backgroundOpacity / 100})`, color: config.primaryColor, fontFamily: config.fontFamily, fontSize: config.fontSize, fontWeight: 800, lineHeight: 1.25, textAlign: "center" }}>{segment.text}</div>; }
