import type { SubtitleTemplateConfig } from "../types";
const config: SubtitleTemplateConfig = { fontFamily: "Microsoft YaHei", fontSize: 72, primaryColor: "#FFFFFF", accentColor: "#B5FF70", position: { x: 50, y: 67 }, maxLines: 2, animationSpeed: 0.9, backgroundOpacity: 74, outlineEnabled: false, shadowEnabled: false };
export default config;
