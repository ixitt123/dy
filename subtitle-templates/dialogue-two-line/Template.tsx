import type { SubtitleTemplateProps } from "../types";
import { activeSegmentIndex } from "../helpers";
export default function DialogueTwoLineTemplate({ segments, currentTimeMs, config }: SubtitleTemplateProps) { const index = activeSegmentIndex(segments, currentTimeMs); if (index < 0) return null; const segment = segments[index]; return <div className="subtitle-dialogue-two-line" style={{ position: "absolute", left: "9%", right: "9%", top: `${config.position.y}%`, transform: "translateY(-50%)", borderRadius: ".45em", padding: ".9em 1em .8em", background: `rgba(13,17,24,${config.backgroundOpacity / 100})`, color: config.primaryColor, fontFamily: config.fontFamily, fontSize: config.fontSize, lineHeight: 1.28 }}><div style={{ marginBottom: ".35em", color: config.accentColor, fontSize: ".48em", fontWeight: 800, letterSpacing: ".04em" }}>{segment.speaker || `讲述 ${index + 1}`}</div><div style={{ fontWeight: 750 }}>{segment.text}</div></div>; }
