import type { SubtitleTemplateProps } from "../types";
import { activeSegmentIndex } from "../helpers";
export default function DocumentaryMinimalTemplate({ segments, currentTimeMs, config }: SubtitleTemplateProps) { const index = activeSegmentIndex(segments, currentTimeMs); if (index < 0) return null; const segment = segments[index]; const enter = Math.min(1, (currentTimeMs - segment.startMs) / 220); return <div className="subtitle-documentary-minimal" style={{ position: "absolute", left: "12%", right: "12%", top: `${config.position.y}%`, transform: "translateY(-50%)", opacity: enter, color: config.primaryColor, fontFamily: config.fontFamily, fontSize: config.fontSize, fontWeight: 480, lineHeight: 1.42, letterSpacing: ".025em", textAlign: "center", textShadow: "0 .08em .24em rgba(0,0,0,.72)" }}>{segment.text}</div>; }
