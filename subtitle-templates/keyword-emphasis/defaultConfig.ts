import type { SubtitleTemplateConfig } from "../types";
const config: SubtitleTemplateConfig = { fontFamily: "Microsoft YaHei", fontSize: 96, primaryColor: "#FFFFFF", accentColor: "#FFDD55", position: { x: 50, y: 60 }, maxLines: 2, animationSpeed: 0.92, backgroundOpacity: 0, outlineEnabled: true, shadowEnabled: true };
export default config;
