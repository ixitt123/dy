import type { SubtitleTemplateProps } from "../types";
import { activeSegmentIndex } from "../helpers";
export default function KeywordTagsTemplate({ segments, currentTimeMs, config }: SubtitleTemplateProps) { const index = activeSegmentIndex(segments, currentTimeMs); if (index < 0) return null; const segment = segments[index]; return <div className="subtitle-keyword-tags" style={{ position: "absolute", left: `${config.position.x}%`, top: `${config.position.y}%`, width: "82%", transform: "translate(-50%,-50%)", color: config.primaryColor, fontFamily: config.fontFamily, fontSize: config.fontSize, fontWeight: 820, lineHeight: 1.25, textAlign: "center" }}><div>{segment.text}</div><div style={{ display: "flex", justifyContent: "center", flexWrap: "wrap", gap: ".35em", marginTop: ".72em", fontSize: ".48em" }}>{(segment.emphasisWords || []).slice(0, 3).map((word) => <span key={word} style={{ borderRadius: "999px", padding: ".32em .68em", color: "#101216", background: config.accentColor, fontWeight: 900 }}>{word}</span>)}</div></div>; }
