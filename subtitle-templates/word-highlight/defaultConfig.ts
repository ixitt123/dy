import type { SubtitleTemplateConfig } from "../types";
const config: SubtitleTemplateConfig = { fontFamily: "Microsoft YaHei", fontSize: 94, primaryColor: "#FFFFFF", accentColor: "#FFD84D", position: { x: 50, y: 61 }, maxLines: 2, animationSpeed: 1, backgroundOpacity: 0, outlineEnabled: true, shadowEnabled: true };
export default config;
